<h1>All Groups</h1>

<% @groups.each do |group| %>
  <div>
    <ul>
      <li><%= link_to group.name, group_path(group) %></li>
      <li><%= group.description[0..100] %>...<br></li>
      <li>Members: <%= group.users.count %></li>
    </ul>
  </div>
<% end %>
